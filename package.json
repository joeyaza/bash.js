{
  "name": "bash-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "rm -rf dist && npm run tsc && jest",
    "start": "rm -rf dist && npm run tsc && node dist/index.js",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^24.9.1",
    "@types/node": "^12.12.58",
    "jest": "^24.9.0",
    "keypress": "^0.2.1",
    "typescript": "^3.9.7"
  },
  "jest": {
    "testMatch": [
      "**/__tests__/**/*.js?(x)",
      "**/?(*.)+(spec|test).js?(x)",
      "!**/?(*.)+(mock).js?(x)"
    ],
    "collectCoverage": true
  }
}
